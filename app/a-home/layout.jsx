export const metadata = {
  title: "FCO Digital Services",
  description: "FCO Digital Services",
};

export default function DashboardLayout({ children }) {
  return <section className="W-full bg-red-500">{children}</section>;
}
