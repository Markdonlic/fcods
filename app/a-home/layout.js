export const metadata = {
  title: "FCO Digital Services",
  description: "FCO Digital Services",
};

export default function Layout({ children }) {
  return (
    <div className="w-full flex justify-center bg-gray-100">{children}</div>
  );
}
